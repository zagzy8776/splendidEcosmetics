{
  "name": "splendid-empire-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "db:push": "npx prisma db push",
    "db:seed": "node seed.js",
    "build": "npx prisma generate && npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.5.2",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
